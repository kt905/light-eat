# ── 依赖 ──
node_modules/
server/node_modules/

# ── 密钥 / 本地配置（绝不提交）──
.env
server/.env

# ── 本地 CLIP 模型（约 150MB，超过 Git 单文件 100MB 上限）──
# 前端会在运行时自动从 /hf-proxy（后端代理 hf-mirror）或 huggingface.co 下载
assets/models/

# ── 日志 / 系统文件 ──
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# 注意：css/tailwind.css 是构建产物，但需要提交（勿在此忽略）
