# ── 依赖 ──
node_modules/
server/node_modules/

# ── 密钥 / 本地配置（绝不提交）──
.env
server/.env

# ── 本地 CLIP 模型（v4 拆分文件均 <100MB，随仓库提交，GitHub Pages 同源自托管）──
# 勿在其中忽略 assets/models/：该目录包含识别模型，提交后静态托管即可直接加载
# （若需重新本地下载，运行 download-clip-model.ps1）

# ── 日志 / 系统文件 ──
*.log
npm-debug.log*
.DS_Store
Thumbs.db

# 注意：css/tailwind.css 是构建产物，但需要提交（勿在此忽略）
